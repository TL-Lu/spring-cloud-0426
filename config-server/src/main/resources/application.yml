server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TL-Lu/lutenglong-springcloud-0423.git
          search-paths:
            - configfiles
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8764/eureka/